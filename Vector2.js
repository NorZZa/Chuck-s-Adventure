var Vector2 = function()
{
	this.x = 0;
	this.y = 0;
};

Vector2.prototype.set = function(x,y)
{
	this.x = y;
	this.y = y;
}

//todos
// Normalize() im fairly certain this will = nothing 
//add
//subtract
//multiplyScalar(num)